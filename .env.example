# Local development (Postgres running on your machine):
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/lobby?schema=public"

# For the deployed demo, swap this for a hosted Postgres URL (Supabase/Neon)
# with connection pooling — nothing else in the app changes.
